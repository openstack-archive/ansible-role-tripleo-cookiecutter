- project:
    templates:
      - release-openstack-server
    check:
      jobs:
        - openstack-tox-linters
        - tripleo-ci-cookiecutter
    gate:
      jobs:
        - openstack-tox-linters
        - tripleo-ci-cookiecutter
    post:
      jobs:
        - publish-openstack-python-branch-tarball
